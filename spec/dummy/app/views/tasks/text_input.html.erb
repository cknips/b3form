<%= b3_form_for @task, html: { id: 'form-1' } do |f| %>
  <%= f.input :text, :title %>
<% end %>

<%= b3_form_for @task, html: { id: 'form-2' } do |f| %>
  <%= f.input :text, :title, label_html: { id: 'label_id' } %>
<% end %>

<%= b3_form_for @task, html: { id: 'form-3' } do |f| %>
  <%= f.input :text, :title, label_html: { class: 'col-lg-2' } %>
<% end %>

<%= b3_form_for @task, html: { id: 'form-4' } do |f| %>
  <%= f.input :text, :title, input_html: { placeholder: 'text' } %>
<% end %>

<%= b3_form_for @task, html: { id: 'form-5' } do |f| %>
  <%= f.input :text, :title, input_html: { class: 'input-lg' } %>
<% end %>

<%= b3_form_for @task, html: { id: 'form-6' } do |f| %>
  <%= f.input :text, :title, wrapper_html: { id: 'wrapper_id' } %>
<% end %>

<%= b3_form_for @task, html: { id: 'form-7' } do |f| %>
  <%= f.input :text, :title, wrapper_html: { class: 'has-success' } %>
<% end %>
